mixin patron(patron = {})
  tr
    td
      a(href=`/patron/details/${patron.id}`) #{`${patron.first_name} ${patron.last_name}`}
    td= patron.address
    td= patron.email
    td= patron.library_id
    td= patron.zip_code